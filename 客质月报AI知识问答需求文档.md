# 客质月报 AI 知识问答需求文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | C Hive Sage｜指标顾问 |
| 文档版本 | V2.1 |
| 文档日期 | 2026-09-20 |
| 目标载体 | Power BI 客质月报 |
| 原型名称 | 客质月报交互设计v2 |
| 当前阶段 | 交互原型与技术可行性定义 |

## 2. 背景与问题

客质月报中的复购率、招新率、客数等前端指标由多层数据处理和复杂代码逻辑计算得出。业务用户看到指标后，往往需要进一步确认计算口径，例如：

- 同一会员一天产生多笔订单时，复购计算按一单还是多单处理？
- 退款、取消、赠品单、跨渠道订单是否纳入？
- 当前页面筛选条件是否影响指标口径？
- 指标同比、今年、去年使用的是自然年、滚动周期还是指定报表月？

目前用户通常需要向内部数据或技术人员咨询，再由相关人员检索代码、文档并人工回复。该过程响应慢、口径容易因人而异，也难以沉淀高频问题。

## 3. 产品目标

在 Power BI 报表内提供不跳转页面的 AI 知识问答能力。用户可在阅读报表时针对当前指标、页面和筛选条件直接提问，由 AI 基于经过治理的知识库给出可追溯回答。

### 3.1 核心目标

1. 降低业务用户理解复杂指标逻辑的门槛。
2. 将人工查询代码、文档和答疑的过程转化为秒级自助问答。
3. 保证回答来源可追溯，并明确知识有效期和责任人。
4. 自动识别当前报表身份，并由用户主动选择问答范围，减少上下文误判。
5. 在无法确认答案时明确拒答或转人工，不生成无依据结论。

### 3.2 非目标

首期不包含以下能力：

- 让 AI 直接修改 Power BI 模型、DAX、SQL 或数据源。
- 根据自然语言自动调整报表筛选器或生成新报表。
- 对报表数据进行开放式预测、归因或经营决策。
- 将未经审核的聊天回答自动写回正式知识库。

## 4. 用户与使用场景

### 4.1 目标用户

| 用户 | 核心诉求 |
|---|---|
| 品牌及业务人员 | 快速理解指标含义、计算方式和异常处理规则 |
| CRM 运营人员 | 确认会员、复购、招新等业务口径 |
| 数据分析人员 | 减少重复答疑，定位口径来源和版本 |
| 数据产品/开发人员 | 管理知识、追踪问题、发现文档缺口 |

### 4.2 典型场景

用户点击右下角“指标顾问”打开助手，在“当前报表”中选择正在咨询的报表。系统根据报表名称路由至对应知识包，并动态生成该报表的快捷问题。Customer Type 已接入 `Customer Type知识包`，可返回完整模拟口径；Product、Member Tier、Binding 和 NPS 在正式知识包接入前仅演示路由、推荐问题和有效问题反馈，不编造业务结论。

## 5. 产品原则

1. **留在报表中**：助手以右侧抽屉覆盖或压缩报表可视区域，不打开新页面。
2. **上下文明确**：使用已绑定的报表身份和用户主动选择的问答范围，不推断 Power BI 页面筛选状态。
3. **知识有据**：正式回答必须来自用户有权限访问且已发布的知识。
4. **结论先行**：先给直接答案，再展示详细口径、例外和来源。
5. **不确定即说明**：知识缺失、冲突或过期时标注风险并提供反馈入口。
6. **权限一致**：AI 不得泄露用户在 Power BI 或知识系统中无权访问的信息。

## 6. 当前实现基线

V2.0 需求正文以 `客质月报交互设计v2.html` 当前可见、可操作行为为唯一原型基线。未来生产能力必须单独标记，不得写成原型已实现能力。

### 6.1 整体交互

- 采用“单一入口 + 右侧覆盖式抽屉”。唯一入口为报表右下角“指标顾问”悬浮按钮，KPI 区域没有额外 AI 入口。
- 入口上方默认显示“不懂指标口径？问问 AI”提示；首次打开助手后隐藏。
- 抽屉宽度桌面端为 430px，1366px 以下为 410px；打开时使用半透明遮罩，但底层报表仍可见。
- 助手使用炭黑、香槟金和暖灰色，与报表视觉保持一致。

### 6.2 右侧助手结构

1. 顶栏：C Hive Sage、指标顾问、连接状态、清空和关闭；没有最小化按钮。
2. 知识状态条：展示当前知识包名称及“已加载/知识包待接入”。
3. 当前报表：报表选择器、收起/展开按钮及说明文案。
4. 对话区：欢迎语、用户问题、加载状态、回答及异常分流。
5. 输入区：500 字文本框、发送按钮和免责声明。

## 7. 功能需求

### FR-01 打开与关闭助手

- 点击右下角“指标顾问”执行打开助手；更新 `aria-hidden`、`aria-expanded`，并在 280ms 后聚焦输入框。
- 点击关闭按钮、点击抽屉外遮罩或按 `Escape` 均可关闭助手。
- 关闭后焦点返回打开助手前的元素；关闭不清空报表选择、折叠状态或历史消息。

### FR-02 当前报表与知识路由

- 模块标题固定为“当前报表”，展开态不重复显示同名字段标签。
- 默认选择 `Customer Type`；可选 `Product`、`Member Tier`、`Binding`、`NPS`。
- 助手不读取底层 Power BI 的 Brand、Period、Report Month、Customer Type 或 Counter 筛选值；选择器仅决定问答知识范围，不改变报表数据。
- 切换报表执行 `handleReportChange`：同步更新知识包名称、知识状态和快捷问题，并显示“已切换至 {reportName}，问答知识范围及推荐问题已更新”的非阻塞提示。
- 每次发送前保存 `contextSnapshot`；历史回答继续展示提问时所选报表，不随之后的报表切换回写。

| 报表 | 展示知识包 | 状态 | 动态快捷问题主题 |
|---|---|---|---|
| Customer Type | Customer Type知识包 | 已加载 | 购买频次、大单、新客、LFL、时间周期 |
| Product | Product 知识包 | 知识包待接入 | 产品新客、产品复购、TOP N、明星产品 |
| Member Tier | Member Tier 知识包 | 知识包待接入 | 等级定义、等级更新、等级与产品 |
| Binding | Binding 知识包 | 知识包待接入 | 绑定定义、绑定状态、渠道规则 |
| NPS | NPS 知识包 | 知识包待接入 | NPS 定义、计算、样本范围 |

### FR-03 当前报表收起/展开

- 默认展开；标题右侧按钮显示“收起⌃”，字号 10px。
- 点击后收起选择器和说明，显示摘要“当前报表：{reportName}”，按钮改为“展开⌄”。
- 再次点击恢复展开；动画最长 220ms，折叠状态在当前页面会话中保持。
- 按钮必须支持键盘操作，并提供 `aria-expanded` 和 `aria-controls`。

### FR-04 欢迎语与动态快捷问题

- 欢迎语固定为：“请基于所选报表进行相关的逻辑问询，若您的有效问题知识库未收录，我将自动反馈给报表负责人。”
- 快捷问题标题固定为“猜你想问”。
- `renderQuickQuestions` 根据当前报表配置生成动态快捷问题；切换报表后不得保留上一报表问题。
- 点击任一快捷问题立即发送；回答下方的推荐追问使用相同交互。

### FR-05 自由输入与发送问题

- 输入框最多 500 个字符；空白问题或回答生成期间的重复发送不处理。
- 点击发送按钮或按 `Enter` 发送；`Shift + Enter` 保留换行能力。
- 发送后立即展示用户消息、清空输入框、禁用发送按钮，并显示“正在核对问题范围与已审核口径”的加载状态。
- 原型在 650ms 后移除加载状态、展示模拟回答、重新启用发送按钮并聚焦输入框。

### FR-06 回答结构与知识依据

正常回答包含：原型标签、结论、计算口径、说明、提问时所选报表、可折叠知识依据、推荐追问和有帮助/没帮助反馈。

- `Customer Type` 已配置购买频次、大单、时间周期、新客、LFL、复购、退款、招新和数据更新等模拟答案。
- 所有业务答案均为原型演示口径，上线前必须由业务及数据负责人核准。
- 知识依据名称随提问时所选报表动态展示；例如旧知识标题中的“客质月报”在 Customer Type 回答里展示为“Customer Type”。
- 点击“查看知识依据”展开标题、版本、更新时间和负责人；按钮随后变为“收起知识依据”。

### FR-07 问题相关性与异常分流

- `in_scope_answered`：返回正常结构化回答。
- `in_scope_unanswered`：显示歉意、知识待补充说明、“已提交给报表负责人”和唯一模拟问题编号；前端不显示处理状态、目标响应或负责团队。
- `cross_report`：显示推荐报表卡片；点击“查看对应报表”仅显示原型非阻塞提示，不发生真实页面跳转，也不创建问题编号。
- `out_of_scope`：提示问题与所选报表相关性较低，给出可提问示例，不创建问题编号。
- Product、Member Tier、Binding、NPS 当前均为待接入状态，因此其有效问题统一进入 `in_scope_unanswered`，不得生成确定性口径。
- 原型使用关键词规则执行分流；生产版本应使用报表目录、语义分类、检索结果与置信度综合判断。

### FR-08 模拟问题编号与后台闭环

- 原型通过 `createTicket` 生成 `KQ-20260911-XXXX` 格式的递增模拟问题编号，并明确标注“后台提交为模拟效果”。
- 原型不连接后台、不会真实提交；生产版本必须创建持久化问题记录，并保存问题、用户、`contextSnapshot`、检索记录、模型与知识版本。
- 后台可维护负责人、优先级、处理状态和时限，但这些字段不得显示在普通用户的知识待补充卡片中。

### FR-09 回答反馈与非阻塞提示

- 每条回答提供“有帮助/没帮助”两个按钮；点击后只保留一个选中状态。
- “有帮助”显示“感谢反馈，已标记为有帮助”；“没帮助”显示“已记录反馈，原型不会真实提交”。
- `showToast` 显示非阻塞提示并在 1800ms 后自动消失；原型不收集没帮助原因，也不真实落库。
- 生产版本可保存问题、答案、引用、上下文、用户和时间；更细反馈原因属于后续增强，不属于当前原型。

### FR-10 清空对话与会话状态

- 点击顶栏清空按钮删除所有用户和助手消息，保留欢迎卡、当前报表选择和折叠状态。
- 清空后显示“对话已清空”的非阻塞提示并聚焦输入框。
- 关闭并重新打开助手保留当前页面生命周期内的对话；刷新页面后恢复默认状态。

### FR-11 无障碍与键盘

- 抽屉、入口、按钮、输入框和状态区必须保留语义化标签及 ARIA 状态。
- 支持 `Enter` 发送、`Shift + Enter` 换行、`Escape` 关闭。
- 动态消息使用 `aria-live="polite"`，状态提示使用 `role="status"`。

### FR-12 单文件交付

- 最新报表 PNG 以 Base64 Data URL 内嵌，HTML 不依赖外部图片、脚本、样式或网络服务。
- 单个 HTML 文件可离线、跨设备打开；底图保持 1576 × 807 原始比例，不得非等比拉伸。

## 8. 知识库需求

### 8.1 知识来源

- 指标口径文档、数据字典、业务规则文档。
- DAX、SQL、ETL 或其他计算逻辑的人工解释版。
- 报表说明、版本发布记录、常见问题和已审核答疑。
- 数据负责人、适用品牌、渠道、市场和时间范围。

不建议直接让大模型读取全部生产代码后自由解释。代码可以作为核验来源，但应沉淀成经过负责人审核、可版本化的知识条目。

### 8.2 知识条目元数据

每个条目至少包含：

- `knowledge_id`、标题、主题域、指标 ID、报表 ID。
- 业务定义、计算公式、计算粒度、包含项、排除项、例外规则。
- 适用品牌、渠道、地区、时间范围。
- 来源系统、原文链接、版本、生效日期、失效日期。
- 内容负责人、审核人、审核状态、更新时间。
- 权限标签和敏感级别。

### 8.3 知识治理

- 仅“已发布且在有效期内”的条目进入正式问答索引。
- 口径变更触发重新审核、重建索引和历史版本保留。
- 定期统计无答案、高频问题和低满意度回答，补充知识缺口。

### 8.4 当前 Customer Type 知识包

当前原型绑定知识包 `monthly_weekly_customers_and_product_and_sales_bigtable-code解析.md`，知识版本日期为 2026-09-08。由于该文件同时包含客质、产品、明星产品、TOP N、会员等级、销售大表和竞品七类主题，问答服务不得将整份文件无差别提供给当前报表。

客质月报允许直接回答的主题包括：

- 客数、销售额、订单数、购买频次、单件率、平均年龄等客质 KPI。
- 新客、老客、潜客、潜转新、WINBACK、Guest 等顾客类型。
- MTD、YTD、R12、WTD、月报与周报截止日期。
- TTL、Channel、Subchannel、Sub-subchannel、Region、Director、City、Counter、BA 和 LFL 等维度。
- 有效订单、退款、大单、会员去重、LFL 区间和数据刷新机制。

知识包已确认的示例口径包括：

- 购买频次为 `orders / customers`，即净交易数除以去重会员数。
- 月报支持 MTD、YTD、R12；周报的 R12 为空，数据截止上周。
- 客质月报默认过滤 `default_flag=1` 的大单、团购、游客或小样等订单；LRL 存在品牌特殊规则，回答必须显示适用范围提示。
- 新客依据品牌首次有效购买是否落在统计区间内判断。
- LFL 依据当年有效区间与去年区间平移后的交集计算。

### 8.5 面向用户的技术信息边界

业务用户不需要理解 SQL 实现。默认回答界面仅展示：业务结论、业务公式、关键包含/排除规则、适用筛选范围、知识条目名称、业务版本、更新时间和业务负责人。

以下信息不得出现在普通用户回答中：SQL 文件名、代码行号、数据库 catalog、schema、表名、中间表、宏、UDF、分区字段、查询片段和调度路径。

技术来源仍应作为后台 `technical_provenance` 保存，供数据开发、知识审核和问题复现使用。管理员可以在受权限保护的治理后台查看，Power BI 前端不提供展开入口。

### 8.6 多报表知识架构

未来几十张报表采用“报表知识包 + 公共知识层 + 中央知识路由器”，避免单一总库产生跨报表口径污染，也避免每个报表重复维护公共规则。

1. **报表知识包**：保存仅适用于某张报表或报表族的指标定义、例外、页面映射和负责人。
2. **公共知识层**：保存经审核、可跨报表复用的时间周期、订单有效性、会员身份和通用维度定义。
3. **中央知识路由器**：根据用户权限、用户选择的 `report_id`、知识包状态和问题语义选择检索范围。
4. **相关报表目录**：记录相邻主题及目标报表，用于识别“知识存在但不属于当前报表”的问题并提供导航。

### 8.7 报表知识包配置规范

每张报表必须注册以下配置：

```json
{
  "reports": {
    "customer_type": {
      "displayReportName": "Customer Type",
      "knowledgePackageId": "kb-crm-customer-core",
      "knowledgeStatus": "ready",
      "ownerTeam": "CRM 数据产品组",
      "quickQuestionSet": "customer_type_v1"
    },
    "product": {
      "displayReportName": "Product",
      "knowledgePackageId": null,
      "knowledgeStatus": "pending",
      "ownerTeam": "CRM 产品报表组",
      "quickQuestionSet": "product_v1"
    }
  }
}
```

完整配置按相同结构注册 Member Tier、Binding 和 NPS。配置使用稳定技术 ID 关联知识包，不依赖页面展示名称。知识文件路径由知识服务管理，前端不得接收本地文件路径。

### 8.8 知识检索顺序与冲突优先级

检索顺序为：当前指标专属知识 → 当前页面知识 → 当前报表知识包 → 公共知识层 → 相关报表目录识别。

冲突处理优先级为：当前报表专属且有效的已发布口径，高于公共知识；更具体的指标/页面范围高于报表级范围；生效日期必须覆盖当前报表周期。相同范围的知识发生冲突时停止生成唯一结论，创建知识冲突工单。

相关报表知识只能用于判断归属和生成导航文案，默认不得用于回答当前报表的口径。

### 8.9 新报表接入流程

1. 注册报表、页面、视觉对象和指标稳定 ID。
2. 选择可复用的公共知识包，并创建报表专属知识包。
3. 从代码解析、口径文档和业务规则中生成候选知识条目。
4. 由数据负责人和业务负责人审核定义、例外、适用期和权限。
5. 建立当前报表问题集、跨报表问题集和范围外问题集。
6. 发布索引并在测试环境验证上下文路由、引用、拒答和权限。
7. 达到验收阈值后绑定生产报表；后续变更通过版本流程发布。

### 8.10 批量质量评测

每张报表上线前至少配置以下评测集：核心指标标准问题、同义表达、筛选相关问题、边界与例外问题、知识缺失问题、其他报表问题、范围外问题和越权问题。

平台级监控应支持按报表和知识包查看准确率、引用正确率、跨报表误答率、知识缺失率、范围外拦截率、工单量、知识版本和用户反馈。新增或更新公共知识包时，必须回归所有依赖它的报表。

## 9. Power BI 集成设计

### 9.1 推荐生产形态

若必须在 Power BI 报表页面内直接完成交互，推荐采用 **Power BI 自定义视觉对象（Custom Visual）** 承载 AI 助手入口和面板。首期自定义视觉对象负责识别绑定的报表/知识包、渲染聊天界面、收集用户主动选择的问答范围，并调用企业后端问答 API；不依赖读取 Power BI 页面筛选器。

原型中的右侧抽屉用于表达目标体验；实际自定义视觉对象受其在画布中的容器范围约束。因此生产报表需预留右侧助手区域，或采用“折叠小卡片/展开大面板”的画布布局。若希望真正覆盖整个 Power BI 页面，需要在企业门户中通过 Power BI Embedded 承载报表，并由宿主页面实现全局抽屉。

### 9.2 两种落地架构

| 方案 | 适用条件 | 优点 | 限制 |
|---|---|---|---|
| Power BI 自定义视觉对象 | 用户直接在 Power BI Service/App 查看报表 | 体验位于报表内，可获得视觉对象数据上下文 | 面板受视觉对象容器限制；开发、签名和组织发布有管理成本 |
| Power BI Embedded + 宿主网页助手 | 企业已有统一数据门户或允许建设门户 | 可实现真正覆盖式侧栏、深度控制报表事件和统一会话 | 需要 Embedded 容量、宿主开发、认证和运维 |

首期建议以自定义视觉对象验证价值；若对全屏覆盖、跨报表会话和复杂交互要求较高，再升级为 Embedded 方案。

### 9.3 逻辑链路

1. 用户点击右下角“指标顾问”打开助手。
2. 前端读取用户选择的报表，并从路由配置取得知识包、知识状态、负责团队和快捷问题。
3. 前端将问题与结构化上下文发送至企业 AI 网关。
4. 网关执行身份验证、权限校验、敏感信息处理和限流。
5. 检索服务基于报表 ID、知识包、用户问答范围、有效期和权限过滤知识。
6. 大模型仅使用获准知识片段组织回答，并返回引用和置信状态。
7. 前端展示答案、来源、更新时间和反馈入口。
8. 审计系统记录请求、使用的知识版本、响应状态和用户反馈。

### 9.4 接口建议

请求示例：

```json
{
  "question": "复购一天多单算一单还是多单？",
  "conversationId": "uuid",
  "locale": "zh-CN",
  "reportContext": {
    "workspaceId": "workspace-id",
    "reportId": "customer_type",
    "reportName": "Customer Type",
    "knowledgePackageId": "kb-crm-customer-core",
    "knowledgeStatus": "ready",
    "source": "user_selected_in_assistant"
  }
}
```

响应示例：

```json
{
  "answer": "……",
  "confidence": "high",
  "scopeNote": "适用于 Customer Type 报表",
  "citations": [
    {
      "knowledgeId": "crm-repurchase-001",
      "title": "CRM 复购指标口径",
      "section": "订单聚合规则",
      "version": "3.2",
      "updatedAt": "2026-08-18",
      "owner": "CRM 数据产品组"
    }
  ],
  "suggestedQuestions": ["退款订单是否计入？", "跨渠道购买如何合并？"]
}
```

对未回答但与报表相关的问题，响应还应包含：

```json
{
  "relevanceStatus": "in_scope_unanswered",
  "answer": "知识库中暂未找到可靠口径，问题已提交给报表负责人。",
  "ticket": {
    "ticketId": "KQ-20260911-0018",
    "status": "待分派",
    "ownerTeam": "CRM 数据产品组",
    "targetResponseAt": "2026-09-15T18:00:00+08:00"
  }
}
```

对报表范围外问题返回 `relevanceStatus: "out_of_scope"`，不得创建 `ticket`。

生产响应可包含 `status`、`ownerTeam` 和 `targetResponseAt` 供后台治理，但当前原型及普通用户反馈卡不得展示这些字段。

## 10. 安全、权限与合规

- 统一使用企业身份认证，后端验证访问令牌，不信任前端传入的用户角色。
- 检索前执行文档级或片段级权限过滤。
- 不向大模型发送非必要的客户明细、会员 ID、手机号等个人信息。
- 问答服务只解释口径，不返回超出当前用户数据权限的聚合或明细。
- 记录模型版本、提示模板版本、知识版本和引用，支持审计复现。
- 对提示注入、越权检索、敏感内容输出进行检测和拦截。
- 明确日志保存期限、脱敏规则、删除流程和模型供应商数据策略。

## 11. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 首字响应目标不超过 3 秒，完整回答目标不超过 10 秒；超时显示可恢复提示 |
| 可用性 | 服务异常不影响原 Power BI 报表使用；助手可独立关闭 |
| 准确性 | 核心口径问题须引用已审核知识；上线前建立基准问题集 |
| 可访问性 | 支持键盘操作、清晰焦点、ARIA 标签和足够对比度 |
| 兼容性 | 优先适配 Power BI Service 桌面浏览器，兼顾 1366 px 及以上宽度 |
| 可观测性 | 监控请求量、响应时间、拒答率、引用命中率、满意度和错误率 |
| 扩展性 | 指标、报表和知识库通过稳定 ID 关联，不依赖界面展示文字硬编码 |

## 12. 状态与异常设计

### 12.1 当前原型状态

- **初始态**：展示欢迎语、当前报表和动态快捷问题。
- **生成中**：显示“正在核对问题范围与已审核口径”，禁用发送按钮；当前不提供停止生成。
- **正常回答**：展示结构化答案、报表快照、可折叠知识依据、追问和反馈。
- **知识待补充**：展示歉意、模拟提交确认和问题编号，不展示内部治理字段。
- **跨报表**：展示推荐报表卡片，导航按钮仅触发模拟提示。
- **范围外**：展示相关性提醒和报表内问题示例。
- **报表变化**：显示“已切换至 {reportName}，问答知识范围及推荐问题已更新”。
- **反馈/清空/模拟导航**：显示 1800ms 非阻塞提示。

### 12.2 生产版本增强状态

来源冲突、知识过期、网络错误、无权限、重试和停止生成属于生产版本增强，当前 HTML 未实现。生产实现必须确保助手异常不影响报表，并且不得泄露受限知识元数据。

## 13. 原型范围

HTML 原型为高保真前端演示，不连接真实 Power BI、知识库或大模型。

### 13.1 原型包含

- 直接使用 `/Users/yanjia/Documents/AI_2026/report_ai/客质月报.png` 最新版本作为唯一报表底图，不再使用 HTML/CSS 近似重绘。
- 底图保持原始宽高比，不拉伸；桌面端优先完整显示，小屏设备允许横向滚动。
- 将最新版 `客质月报.png` 转换为 Base64 Data URL 并直接内嵌到 HTML；交付文件不得依赖本地绝对路径、同级图片或网络图片，确保单个 HTML 可跨设备打开。
- 全局 AI 悬浮按钮及右侧助手抽屉。
- 打开、关闭、清空对话和上下文展开/收起。
- 快捷问题、自由输入、发送及模拟生成回答。
- 引用依据展开、推荐追问、满意度反馈和异常演示。
- 响应式布局和基础键盘可用性。
- 报表画布五个筛选器作为视觉展示，不与 AI 自动同步。
- 助手内提供 Customer Type、Product、Member Tier、Binding、NPS 报表选择器。
- 报表名称默认显示“Customer Type”，并由报表路由配置匹配知识包、知识状态、负责人和快捷问题。
- “当前报表”模块支持一键收起；展开态不在选择器上方重复展示“当前报表”标签，收起后显示紧凑报表摘要并扩大聊天区域。
- 快捷问题区域显示“猜你想问”，并随报表选择动态更新。
- 欢迎语使用“请基于所选报表进行相关的逻辑问询”，说明提问范围和知识缺失反馈机制。
- 与报表相关但无答案的问题自动生成模拟问题编号和后台提交状态。
- 与报表无关的问题相关性提示和报表内问题引导。
- 基于 2026-09-08 客质知识包的购买频次、大单、时间周期、新客和 LFL 等模拟回答。
- 产品 TOP N、明星产品、会员等级和竞品等跨报表问题的归属提示及模拟导航卡。
- 普通用户界面仅显示业务口径和业务引用，不展示 SQL、表名、代码路径或技术血缘。

### 13.2 原型不包含

- 真实 Power BI SDK、自定义视觉对象包或 Embedded 认证。
- 真实知识检索、大模型调用、用户权限和审计落库。
- 报表真实筛选、刷新或数据计算。

### 13.3 原型行为—需求映射表

| HTML 行为/函数 | 用户可见行为 | 对应需求 |
|---|---|---|
| `openAssistant` | 打开助手、隐藏入口提示、聚焦输入框 | FR-01 |
| `closeAssistant` | 关闭按钮、遮罩或 Escape 关闭并恢复焦点 | FR-01、FR-11 |
| `renderContext` | 同步更新知识包名称、知识状态和快捷问题 | FR-02 |
| `renderQuickQuestions` | 生成当前报表的动态快捷问题 | FR-04 |
| `toggleContext` | 当前报表收起/展开并保留状态 | FR-03 |
| `sendQuestion` | 发送问题、加载状态、650ms 模拟回答 | FR-05 |
| `pickResponse` | 已回答、知识缺失、跨报表、范围外分流 | FR-06、FR-07 |
| `createTicket` | 生成模拟问题编号 | FR-08 |
| `renderReportRecommendation` | 显示推荐报表卡片及模拟导航提示 | FR-07 |
| `getCitationLabel` | 知识依据名称随提问时所选报表动态展示 | FR-06 |
| `toggleCitation` | 查看知识依据/收起知识依据 | FR-06 |
| `submitFeedback` | 有帮助/没帮助选择及反馈提示 | FR-09 |
| `clearConversation` | 清空对话但保留欢迎卡和报表状态 | FR-10 |
| `showToast` | 展示 1800ms 非阻塞提示 | FR-09 |
| `handleReportChange` | 切换报表并刷新知识路由和推荐问题 | FR-02 |

## 14. 埋点与效果指标

建议记录：助手曝光、打开方式、问题类型、指标 ID、是否命中知识、引用数量、响应耗时、追问率、复制率、正负反馈、转人工率和会话退出。

首期业务指标建议：

- 高频口径问题自助解决率 ≥ 70%。
- 有可靠引用的回答占比 ≥ 95%。
- 已标注基准问题准确率 ≥ 90%。
- 用户正向反馈率 ≥ 80%。
- 人工重复答疑量下降 ≥ 40%。

## 15. 验收标准

### 15.1 原型验收

1. 单个 HTML 可直接在现代浏览器离线打开，无需安装依赖或附带图片文件。
2. 内嵌底图内容与指定最新版 `客质月报.png` 一致，保持 1576 × 807 比例。
3. 仅右下角“指标顾问”入口可打开右侧助手，KPI 区域无额外 AI 图标。
4. 助手可通过入口打开，并可通过关闭按钮、遮罩和 Escape 关闭；关闭后恢复焦点且不清空会话。
5. 默认报表为 Customer Type，并可选择 Product、Member Tier、Binding、NPS。
6. 切换报表后，知识包名称、知识状态、快捷问题和非阻塞提示同步更新。
7. 当前报表模块可收起/展开；摘要、按钮文案、ARIA 状态和会话内折叠状态正确。
8. 欢迎语完整显示确认文案，快捷问题区域显示“猜你想问”。
9. 快捷问题、推荐追问、发送按钮和 Enter 均可发送；Shift + Enter 不发送。
10. 空白问题和回答生成期间的重复发送被阻止；输入框最多 500 字。
11. 发送期间显示加载状态，约 650ms 后恢复发送与输入焦点。
12. 正常回答展示结论、计算口径、说明、报表快照、知识依据、追问和反馈。
13. 知识依据可展开/收起，并显示标题、版本、更新时间和负责人。
14. 知识依据名称按提问时所选报表展示，历史回答不随之后的报表切换回写。
15. Customer Type 的购买频次、大单、时间周期、新客、LFL、复购、退款、招新和更新问题可返回模拟答案。
16. Product、Member Tier、Binding、NPS 的有效问题不生成确定性答案，而是生成模拟问题编号。
17. 知识待补充卡仅显示提交确认和问题编号，不显示处理状态、目标响应或负责团队。
18. 跨报表问题显示推荐报表卡片；点击导航只显示原型提示，不真实跳转、不创建编号。
19. 范围外问题显示相关性提示和示例，不创建编号。
20. 有帮助/没帮助可互斥选中并显示对应提示；明确不会真实提交。
21. 清空对话删除消息但保留欢迎卡、当前报表和折叠状态。
22. 页面和回答不显示 SQL 文件名、底层表名、代码行号或代码片段。

### 15.2 生产版本验收

1. 在目标 Power BI 使用环境中无需跳出报表即可完成问答。
2. 上下文中的报表、页面、指标和筛选条件传递准确。
3. 回答只能引用当前用户有权访问且已发布的知识。
4. 基准问题集满足准确率和引用正确率要求。
5. 知识缺失、冲突、过期、无权限和服务异常均符合预期降级策略。
6. 日志可复现一次回答使用的模型、提示和知识版本。
7. 助手故障不影响报表加载、筛选和浏览。

## 16. 分阶段实施建议

### 阶段 0：原型验证

使用 HTML 原型验证入口位置、侧栏宽度、回答结构、引用展示和用户理解度。

### 阶段 1：最小可用版本

选择 10–20 个高频 CRM 指标，完成知识治理；开发 Power BI 自定义视觉对象和企业问答 API；支持上下文、引用、反馈和审计。

### 阶段 2：扩展与治理

覆盖更多报表和指标，增加知识运营后台、冲突检测、版本提醒、质量评测和负责人闭环。

### 阶段 3：平台化

根据使用效果评估 Power BI Embedded 门户、跨报表会话、统一数据助手及其他 BI 场景。

## 17. 风险与待核准事项

以下事项不影响原型制作，但必须在生产开发前由相关负责人确认：

1. 最终选择 Power BI 自定义视觉对象还是 Power BI Embedded。
2. 自定义视觉对象是否允许访问外部企业 API，以及组织签名与发布流程。
3. 正式知识库产品、内容负责人和发布审核机制。
4. 复购等指标的真实业务规则及代码核验结果。
5. 企业大模型、向量检索、认证网关和日志平台选型。
6. 用户问题、答案及反馈的保存期限和合规要求。

## 18. 设计结论

当前原型采用“右下角 C Hive Sage 指标顾问 + 右侧覆盖式抽屉”，不在 KPI 区域增加入口。助手在“当前报表”中提供 Customer Type、Product、Member Tier、Binding、NPS 选择；选择驱动知识包、知识状态和动态快捷问题。Customer Type 基于参考知识文件提供已写入原型的模拟回答，其余报表在知识包接入前仅演示路由和知识缺失反馈。报表画布筛选器与助手不自动同步，所有后台提交、反馈和导航均明确为模拟效果。
