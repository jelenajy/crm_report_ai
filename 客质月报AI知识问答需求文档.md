# 客质月报 AI 知识问答需求文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | C Hive Lens｜指标顾问 |
| 文档版本 | V1.2 |
| 文档日期 | 2026-09-13 |
| 目标载体 | Power BI 客质月报 |
| 原型名称 | 客质月报交互设计v2 |
| 当前阶段 | 交互原型与技术可行性定义 |

## 2. 背景与问题

客质月报中的复购率、招新率、客数等前端指标由多层数据处理和复杂代码逻辑计算得出。业务用户看到指标后，往往需要进一步确认计算口径，例如：

- 同一会员一天产生多笔订单时，复购计算按一单还是多单处理？
- 退款、取消、赠品单、跨渠道订单是否纳入？
- 当前页面筛选条件是否影响指标口径？
- 指标同比、今年、去年使用的是自然年、滚动周期还是指定报表月？

目前用户通常需要向内部数据或技术人员咨询，再由相关人员检索代码、文档并人工回复。该过程响应慢、口径容易因人而异，也难以沉淀高频问题。

## 3. 产品目标

在 Power BI 报表内提供不跳转页面的 AI 知识问答能力。用户可在阅读报表时针对当前指标、页面和筛选条件直接提问，由 AI 基于经过治理的知识库给出可追溯回答。

### 3.1 核心目标

1. 降低业务用户理解复杂指标逻辑的门槛。
2. 将人工查询代码、文档和答疑的过程转化为秒级自助问答。
3. 保证回答来源可追溯，并明确知识有效期和责任人。
4. 自动携带当前报表上下文，减少用户重复描述。
5. 在无法确认答案时明确拒答或转人工，不生成无依据结论。

### 3.2 非目标

首期不包含以下能力：

- 让 AI 直接修改 Power BI 模型、DAX、SQL 或数据源。
- 根据自然语言自动调整报表筛选器或生成新报表。
- 对报表数据进行开放式预测、归因或经营决策。
- 将未经审核的聊天回答自动写回正式知识库。

## 4. 用户与使用场景

### 4.1 目标用户

| 用户 | 核心诉求 |
|---|---|
| 品牌及业务人员 | 快速理解指标含义、计算方式和异常处理规则 |
| CRM 运营人员 | 确认会员、复购、招新等业务口径 |
| 数据分析人员 | 减少重复答疑，定位口径来源和版本 |
| 数据产品/开发人员 | 管理知识、追踪问题、发现文档缺口 |

### 4.2 典型场景

用户正在“TTL—新客—柜台”视图查看复购率，点击字段旁的 AI 图标。系统自动携带“报表名称、页面、字段、品牌、周期、报表月份、顾客类型、柜台”等上下文并打开右侧助手。用户选择“复购率如何计算”，AI 根据知识库解释公式、订单去重粒度、退款规则和适用范围，同时展示知识来源与更新时间。用户可继续追问“一天多单怎么算”。

## 5. 产品原则

1. **留在报表中**：助手以右侧抽屉覆盖或压缩报表可视区域，不打开新页面。
2. **上下文优先**：优先使用当前报表、页面、视觉对象、指标和筛选条件。
3. **知识有据**：正式回答必须来自用户有权限访问且已发布的知识。
4. **结论先行**：先给直接答案，再展示详细口径、例外和来源。
5. **不确定即说明**：知识缺失、冲突或过期时标注风险并提供反馈入口。
6. **权限一致**：AI 不得泄露用户在 Power BI 或知识系统中无权访问的信息。

## 6. 整体交互方案

采用“全局悬浮入口 + 指标级快捷入口 + 右侧常驻抽屉”的组合方案。助手品牌统一为 **C Hive Lens**，中文功能名称为“指标顾问”；视觉使用炭黑、香槟金和暖灰色，与客质月报保持一致。

### 6.1 全局入口

- 在报表右下角展示“AI 指标助手”悬浮按钮。
- 点击后从右侧打开宽约 400–440 px 的助手抽屉。
- 抽屉打开后报表仍可见；桌面端优先采用覆盖式，避免改变原报表视觉布局。
- 按钮提供未读状态或提示气泡，但不主动遮挡核心数据。

### 6.2 指标级入口

- 在“复购率、招新率、客数”等重点字段或信息提示区域提供 AI 图标。
- 点击后自动打开助手，并注入当前字段及筛选上下文。
- 输入区上方显示上下文标签，例如“复购率 / 新客 / 2025-09 / 柜台”。
- 自动推荐与该字段相关的 3–4 个问题。

### 6.3 右侧助手结构

1. 顶栏：产品名称、知识状态、最小化、清空和关闭。
2. 上下文卡片：当前页面、指标、关键筛选条件，可展开查看全部。
3. 对话区：欢迎语、用户问题、AI 回答、异常提示。
4. 引用区：知识条目名称、版本、更新时间、负责人，可折叠。
5. 快捷追问：基于当前指标展示推荐问题。
6. 输入区：自由输入、发送按钮及“答案仅供口径理解”提示。

## 7. 功能需求

### FR-01 助手打开与关闭

- 用户可通过悬浮按钮或字段级入口打开助手。
- 关闭抽屉不清除本次会话；重新打开后保留上下文和历史消息。
- 切换报表页面或关键筛选器后，系统提示“报表上下文已更新”。

### FR-02 报表上下文获取

上下文至少包括：

- 工作区、报表 ID、报表名称、页面 ID、页面名称。
- 视觉对象 ID 和标题（若从字段级入口进入）。
- 指标技术标识、展示名称和所属主题域。
- 当前可见筛选条件：品牌、Period、Report Month、Customer Type、Counter 等。
- 用户身份、角色和权限声明。
- 当前时间、语言和知识库版本。

系统应区分“计算口径上下文”和“用户筛选上下文”。筛选条件用于限定回答适用范围，不应被错误解释为指标定义本身。

### FR-02A 筛选器实时联动

- Brand、Period、Report Month、Customer Type、Counter 的选择变化应实时同步至助手上下文。
- 筛选发生变化后，助手显示一次非阻塞提示“报表上下文已更新”，并列出发生变化的条件。
- 已生成的历史回答保留其生成时上下文，不随新筛选条件回写；后续问题使用最新上下文。
- 每条回答应保存 `contextSnapshot`，用于审计和问题复现。

### FR-03 快捷问题

- 全局入口默认问题：指标口径、数据更新、异常处理、负责人。
- 指标入口默认问题：如何计算、一天多单规则、退款规则、时间范围。
- 点击快捷问题后直接发送，并显示加载状态。

### FR-04 自由问答

- 支持中文自然语言输入，首期可兼容英文提问。
- 支持连续追问，并保留最近对话上下文。
- 单个问题建议限制在 500 字以内。
- 对超出报表口径范围的问题，说明能力边界并引导用户重新提问。

### FR-05 AI 回答结构

回答优先按以下结构生成：

1. **直接结论**：用 1–2 句话回答用户核心问题。
2. **计算口径**：展示业务公式或文字逻辑。
3. **关键规则**：解释去重、退款、取消、跨渠道、时间边界等。
4. **当前上下文**：说明回答对应的报表页面与筛选条件。
5. **知识依据**：展示知识条目、版本、更新时间和负责人。
6. **风险说明**：存在冲突、过期或信息不足时显著提示。

演示答案示例：复购按“会员 + 自然日”聚合，同一会员同一天多笔有效订单合并为一次购买行为；在统计期内存在不少于两次有效购买行为时计为复购会员。该示例仅用于原型展示，生产上线前必须由业务及数据负责人核准。

### FR-06 引用与溯源

- 每个关键结论至少对应一个知识来源。
- 用户可展开“查看依据”，查看文档标题、章节、版本、更新时间和负责人。
- 有权限时可跳转到知识原文；无权限时仅显示允许公开的元数据。
- 回答中的知识引用应与检索片段建立可审计关联。

### FR-07 低置信度与冲突处理

- 无可用知识：明确显示“知识库中暂未找到可靠答案”。
- 多个来源冲突：不自行选择，展示冲突提示、来源版本和责任人。
- 知识已过有效期：展示“口径可能已更新”提示。
- 提供“提交反馈/联系数据负责人”入口，记录问题和上下文。

### FR-07A 问题相关性识别与分流

问答服务应在知识检索前后综合判断问题与当前报表的相关性，并返回 `relevanceStatus`：

- `in_scope_answered`：与报表相关且存在可靠知识，正常回答。
- `in_scope_unanswered`：与报表逻辑相关，但知识库没有可靠答案。系统自动创建待处理问题并展示问题编号、负责人和状态。
- `cross_report`：问题属于另一张已接入报表。系统说明目标报表名称并提供导航，不将其他报表知识作为当前报表答案，也不创建知识缺失工单。
- `out_of_scope`：与客质月报及其指标逻辑相关性较低。系统不创建后台问题，解释能力边界，并推荐复购、招新、客数、退款、同比和数据刷新等相关问题。
- `ambiguous`：无法判断用户意图时，先提出一个澄清问题，不直接提交后台。

相关性判断不得仅依赖关键词。生产服务应结合报表主题、指标目录、当前上下文和语义分类模型；低置信度分类按 `ambiguous` 处理。

### FR-08 反馈

- 每条回答支持“有帮助/没帮助”。
- “没帮助”可选择：答案不准确、没有回答问题、引用过期、表达不清、其他。
- 反馈记录包括问题、答案、引用、上下文、用户及时间。
- 首期原型仅模拟交互，不真实提交。

### FR-08A 未回答问题后台闭环

对于 `in_scope_unanswered` 问题，生产系统应在用户发送后自动创建问题记录，无需用户二次点击提交。前端展示“已提交给数据负责人”及唯一编号，例如 `KQ-20260911-0018`。

后台记录至少包含：

- `ticket_id`、问题原文、标准化问题、创建时间、用户标识。
- 提问时的报表、页面、视觉对象、指标和完整筛选快照。
- 相关性判断结果、检索关键词、最高匹配分数和候选知识 ID。
- 模型版本、提示模板版本、知识索引版本。
- 负责团队、指定负责人、优先级、处理状态和处理时限。
- 最终答复、关联的新知识条目、审核人和用户通知状态。

状态流转为：`待分派 → 待分析 → 待业务确认 → 已补充知识 → 已回复 → 已关闭`。默认由指标所属主题域负责人接收；无明确指标时分派至 CRM 数据产品组。一般问题目标在 2 个工作日内首次响应，高优先级口径风险在 4 个工作小时内响应。

负责人完成处理后，应先补充或修订知识条目并通过审核，再向原提问用户发送可追溯答复。禁止仅在工单中回复而不沉淀知识。

### FR-09 会话管理

- 支持清空当前会话。
- 默认不长期保存用户完整聊天内容；生产保留周期由企业合规策略确定。
- 新的报表、页面或指标上下文可开启新会话，避免旧上下文污染。

### FR-10 多语言

- 助手界面跟随报表语言设置。
- 中文提问默认中文回答；英文提问可英文回答。
- 知识库原文不翻译时，应标明原始语言。

## 8. 知识库需求

### 8.1 知识来源

- 指标口径文档、数据字典、业务规则文档。
- DAX、SQL、ETL 或其他计算逻辑的人工解释版。
- 报表说明、版本发布记录、常见问题和已审核答疑。
- 数据负责人、适用品牌、渠道、市场和时间范围。

不建议直接让大模型读取全部生产代码后自由解释。代码可以作为核验来源，但应沉淀成经过负责人审核、可版本化的知识条目。

### 8.2 知识条目元数据

每个条目至少包含：

- `knowledge_id`、标题、主题域、指标 ID、报表 ID。
- 业务定义、计算公式、计算粒度、包含项、排除项、例外规则。
- 适用品牌、渠道、地区、时间范围。
- 来源系统、原文链接、版本、生效日期、失效日期。
- 内容负责人、审核人、审核状态、更新时间。
- 权限标签和敏感级别。

### 8.3 知识治理

- 仅“已发布且在有效期内”的条目进入正式问答索引。
- 口径变更触发重新审核、重建索引和历史版本保留。
- 定期统计无答案、高频问题和低满意度回答，补充知识缺口。

### 8.4 当前客质月报知识包

当前原型绑定知识包 `monthly_weekly_customers_and_product_and_sales_bigtable-code解析.md`，知识版本日期为 2026-09-08。由于该文件同时包含客质、产品、明星产品、TOP N、会员等级、销售大表和竞品七类主题，问答服务不得将整份文件无差别提供给当前报表。

客质月报允许直接回答的主题包括：

- 客数、销售额、订单数、购买频次、单件率、平均年龄等客质 KPI。
- 新客、老客、潜客、潜转新、WINBACK、Guest 等顾客类型。
- MTD、YTD、R12、WTD、月报与周报截止日期。
- TTL、Channel、Subchannel、Sub-subchannel、Region、Director、City、Counter、BA 和 LFL 等维度。
- 有效订单、退款、大单、会员去重、LFL 区间和数据刷新机制。

知识包已确认的示例口径包括：

- 购买频次为 `orders / customers`，即净交易数除以去重会员数。
- 月报支持 MTD、YTD、R12；周报的 R12 为空，数据截止上周。
- 客质月报默认过滤 `default_flag=1` 的大单、团购、游客或小样等订单；LRL 存在品牌特殊规则，回答必须显示适用范围提示。
- 新客依据品牌首次有效购买是否落在统计区间内判断。
- LFL 依据当年有效区间与去年区间平移后的交集计算。

### 8.5 面向用户的技术信息边界

业务用户不需要理解 SQL 实现。默认回答界面仅展示：业务结论、业务公式、关键包含/排除规则、适用筛选范围、知识条目名称、业务版本、更新时间和业务负责人。

以下信息不得出现在普通用户回答中：SQL 文件名、代码行号、数据库 catalog、schema、表名、中间表、宏、UDF、分区字段、查询片段和调度路径。

技术来源仍应作为后台 `technical_provenance` 保存，供数据开发、知识审核和问题复现使用。管理员可以在受权限保护的治理后台查看，Power BI 前端不提供展开入口。

### 8.6 多报表知识架构

未来几十张报表采用“报表知识包 + 公共知识层 + 中央知识路由器”，避免单一总库产生跨报表口径污染，也避免每个报表重复维护公共规则。

1. **报表知识包**：保存仅适用于某张报表或报表族的指标定义、例外、页面映射和负责人。
2. **公共知识层**：保存经审核、可跨报表复用的时间周期、订单有效性、会员身份和通用维度定义。
3. **中央知识路由器**：根据用户权限、`report_id`、`page_id`、`metric_id`、筛选上下文和问题语义选择检索范围。
4. **相关报表目录**：记录相邻主题及目标报表，用于识别“知识存在但不属于当前报表”的问题并提供导航。

### 8.7 报表知识包配置规范

每张报表必须注册以下配置：

```json
{
  "reportId": "crm-customer-monthly",
  "reportName": "客质月报",
  "reportFamily": "CRM Monthly & Weekly",
  "topics": ["customer", "new", "existing", "winback", "lfl"],
  "knowledgePackages": ["kb-crm-customer-core"],
  "sharedPackages": ["kb-time-period", "kb-valid-order", "kb-member-identity"],
  "allowedMetricIds": ["customers", "orders", "purchase_frequency", "single_qty_rate", "avg_age"],
  "relatedReports": ["crm-product-monthly", "crm-product-topn", "crm-sales-competitor"],
  "ownerTeam": "CRM 数据产品组",
  "permissionTags": ["crm-report-user"]
}
```

配置使用稳定技术 ID 关联 Power BI，不依赖页面展示名称。知识文件路径由知识服务管理，前端不得接收本地文件路径。

### 8.8 知识检索顺序与冲突优先级

检索顺序为：当前指标专属知识 → 当前页面知识 → 当前报表知识包 → 公共知识层 → 相关报表目录识别。

冲突处理优先级为：当前报表专属且有效的已发布口径，高于公共知识；更具体的指标/页面范围高于报表级范围；生效日期必须覆盖当前报表周期。相同范围的知识发生冲突时停止生成唯一结论，创建知识冲突工单。

相关报表知识只能用于判断归属和生成导航文案，默认不得用于回答当前报表的口径。

### 8.9 新报表接入流程

1. 注册报表、页面、视觉对象和指标稳定 ID。
2. 选择可复用的公共知识包，并创建报表专属知识包。
3. 从代码解析、口径文档和业务规则中生成候选知识条目。
4. 由数据负责人和业务负责人审核定义、例外、适用期和权限。
5. 建立当前报表问题集、跨报表问题集和范围外问题集。
6. 发布索引并在测试环境验证上下文路由、引用、拒答和权限。
7. 达到验收阈值后绑定生产报表；后续变更通过版本流程发布。

### 8.10 批量质量评测

每张报表上线前至少配置以下评测集：核心指标标准问题、同义表达、筛选相关问题、边界与例外问题、知识缺失问题、其他报表问题、范围外问题和越权问题。

平台级监控应支持按报表和知识包查看准确率、引用正确率、跨报表误答率、知识缺失率、范围外拦截率、工单量、知识版本和用户反馈。新增或更新公共知识包时，必须回归所有依赖它的报表。

## 9. Power BI 集成设计

### 9.1 推荐生产形态

若必须在 Power BI 报表页面内直接完成交互，推荐采用 **Power BI 自定义视觉对象（Custom Visual）** 承载 AI 助手入口和面板。自定义视觉对象负责获取报表筛选上下文、渲染聊天界面，并调用企业后端问答 API。

原型中的右侧抽屉用于表达目标体验；实际自定义视觉对象受其在画布中的容器范围约束。因此生产报表需预留右侧助手区域，或采用“折叠小卡片/展开大面板”的画布布局。若希望真正覆盖整个 Power BI 页面，需要在企业门户中通过 Power BI Embedded 承载报表，并由宿主页面实现全局抽屉。

### 9.2 两种落地架构

| 方案 | 适用条件 | 优点 | 限制 |
|---|---|---|---|
| Power BI 自定义视觉对象 | 用户直接在 Power BI Service/App 查看报表 | 体验位于报表内，可获得视觉对象数据上下文 | 面板受视觉对象容器限制；开发、签名和组织发布有管理成本 |
| Power BI Embedded + 宿主网页助手 | 企业已有统一数据门户或允许建设门户 | 可实现真正覆盖式侧栏、深度控制报表事件和统一会话 | 需要 Embedded 容量、宿主开发、认证和运维 |

首期建议以自定义视觉对象验证价值；若对全屏覆盖、跨报表会话和复杂交互要求较高，再升级为 Embedded 方案。

### 9.3 逻辑链路

1. 用户打开助手或点击指标入口。
2. 前端采集用户身份、报表标识、页面、视觉对象、指标及筛选条件。
3. 前端将问题与结构化上下文发送至企业 AI 网关。
4. 网关执行身份验证、权限校验、敏感信息处理和限流。
5. 检索服务基于指标 ID、报表 ID、有效期和权限过滤知识。
6. 大模型仅使用获准知识片段组织回答，并返回引用和置信状态。
7. 前端展示答案、来源、更新时间和反馈入口。
8. 审计系统记录请求、使用的知识版本、响应状态和用户反馈。

### 9.4 接口建议

请求示例：

```json
{
  "question": "复购一天多单算一单还是多单？",
  "conversationId": "uuid",
  "locale": "zh-CN",
  "reportContext": {
    "workspaceId": "workspace-id",
    "reportId": "report-id",
    "pageId": "ttl-page",
    "visualId": "customer-type-table",
    "metricId": "repurchase_rate",
    "filters": {
      "brand": "Lancome",
      "period": "MTD",
      "reportMonth": "2025-09",
      "customerType": "新客",
      "counter": "All"
    }
  }
}
```

响应示例：

```json
{
  "answer": "……",
  "confidence": "high",
  "scopeNote": "适用于客质月报 TTL 页面",
  "citations": [
    {
      "knowledgeId": "crm-repurchase-001",
      "title": "CRM 复购指标口径",
      "section": "订单聚合规则",
      "version": "3.2",
      "updatedAt": "2026-08-18",
      "owner": "CRM 数据产品组"
    }
  ],
  "suggestedQuestions": ["退款订单是否计入？", "跨渠道购买如何合并？"]
}
```

对未回答但与报表相关的问题，响应还应包含：

```json
{
  "relevanceStatus": "in_scope_unanswered",
  "answer": "知识库中暂未找到可靠口径，问题已提交给数据负责人。",
  "ticket": {
    "ticketId": "KQ-20260911-0018",
    "status": "待分派",
    "ownerTeam": "CRM 数据产品组",
    "targetResponseAt": "2026-09-15T18:00:00+08:00"
  }
}
```

对报表范围外问题返回 `relevanceStatus: "out_of_scope"`，不得创建 `ticket`。

## 10. 安全、权限与合规

- 统一使用企业身份认证，后端验证访问令牌，不信任前端传入的用户角色。
- 检索前执行文档级或片段级权限过滤。
- 不向大模型发送非必要的客户明细、会员 ID、手机号等个人信息。
- 问答服务只解释口径，不返回超出当前用户数据权限的聚合或明细。
- 记录模型版本、提示模板版本、知识版本和引用，支持审计复现。
- 对提示注入、越权检索、敏感内容输出进行检测和拦截。
- 明确日志保存期限、脱敏规则、删除流程和模型供应商数据策略。

## 11. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 首字响应目标不超过 3 秒，完整回答目标不超过 10 秒；超时显示可恢复提示 |
| 可用性 | 服务异常不影响原 Power BI 报表使用；助手可独立关闭 |
| 准确性 | 核心口径问题须引用已审核知识；上线前建立基准问题集 |
| 可访问性 | 支持键盘操作、清晰焦点、ARIA 标签和足够对比度 |
| 兼容性 | 优先适配 Power BI Service 桌面浏览器，兼顾 1366 px 及以上宽度 |
| 可观测性 | 监控请求量、响应时间、拒答率、引用命中率、满意度和错误率 |
| 扩展性 | 指标、报表和知识库通过稳定 ID 关联，不依赖界面展示文字硬编码 |

## 12. 状态与异常设计

- **初始态**：展示能力说明、当前上下文和推荐问题。
- **生成中**：显示“正在查找已审核口径”，允许停止生成。
- **正常回答**：展示结构化答案、引用和追问。
- **无答案**：说明未找到可靠知识，并提供反馈或负责人信息。
- **来源冲突**：显示冲突警示和多个来源，不输出唯一结论。
- **上下文变化**：提示筛选条件已更新，后续回答使用新上下文。
- **网络/服务错误**：保留用户问题并提供重试，不影响报表。
- **无权限**：说明无法访问相关知识，不透露受限内容标题或摘要。

## 13. 原型范围

HTML 原型为高保真前端演示，不连接真实 Power BI、知识库或大模型。

### 13.1 原型包含

- 参考客质月报的顶部栏、左侧导航、筛选区和指标表格。
- 全局 AI 悬浮按钮及右侧助手抽屉。
- 复购率等字段旁的 AI 快捷入口。
- 打开、关闭、清空对话和上下文展开/收起。
- 快捷问题、自由输入、发送及模拟生成回答。
- 引用依据展开、推荐追问、满意度反馈和异常演示。
- 响应式布局和基础键盘可用性。
- 五个筛选器的模拟选项、实时上下文同步及更新提示。
- 与报表相关但无答案的问题自动生成模拟问题编号和后台提交状态。
- 与报表无关的问题相关性提示和报表内问题引导。
- 基于 2026-09-08 客质知识包的购买频次、大单、时间周期、新客和 LFL 等模拟回答。
- 产品 TOP N、明星产品、会员等级和竞品等跨报表问题的归属提示及模拟导航卡。
- 普通用户界面仅显示业务口径和业务引用，不展示 SQL、表名、代码路径或技术血缘。

### 13.2 原型不包含

- 真实 Power BI SDK、自定义视觉对象包或 Embedded 认证。
- 真实知识检索、大模型调用、用户权限和审计落库。
- 报表真实筛选、刷新或数据计算。

## 14. 埋点与效果指标

建议记录：助手曝光、打开方式、问题类型、指标 ID、是否命中知识、引用数量、响应耗时、追问率、复制率、正负反馈、转人工率和会话退出。

首期业务指标建议：

- 高频口径问题自助解决率 ≥ 70%。
- 有可靠引用的回答占比 ≥ 95%。
- 已标注基准问题准确率 ≥ 90%。
- 用户正向反馈率 ≥ 80%。
- 人工重复答疑量下降 ≥ 40%。

## 15. 验收标准

### 15.1 原型验收

1. HTML 单文件可直接在现代浏览器打开，无需安装依赖。
2. 视觉结构与参考客质月报一致，且不直接复制品牌受限资产作为实现依赖。
3. 悬浮入口和字段入口均可打开右侧助手。
4. 点击“复购率”入口后，助手可展示该指标及当前筛选上下文。
5. 至少三个快捷问题和自由输入可触发不同模拟回答。
6. 可展开知识来源，显示版本、更新时间和负责人。
7. 可演示无答案/低置信度提示、反馈、清空和关闭。
8. 修改任一筛选器后，助手上下文实时更新并出现提示。
9. 与报表相关但无答案的问题生成唯一模拟编号，且显示负责团队和处理状态。
10. 与报表无关的问题不生成编号，并展示相关性引导和推荐问题。
11. 购买频次、大单、时间周期等问题可返回与当前知识包一致的业务答案。
12. 其他已接入报表的问题显示目标报表卡片，不创建知识缺失工单。
13. 页面与回答中不出现 SQL 文件名、底层表名、代码行号或代码片段。

### 15.2 生产版本验收

1. 在目标 Power BI 使用环境中无需跳出报表即可完成问答。
2. 上下文中的报表、页面、指标和筛选条件传递准确。
3. 回答只能引用当前用户有权访问且已发布的知识。
4. 基准问题集满足准确率和引用正确率要求。
5. 知识缺失、冲突、过期、无权限和服务异常均符合预期降级策略。
6. 日志可复现一次回答使用的模型、提示和知识版本。
7. 助手故障不影响报表加载、筛选和浏览。

## 16. 分阶段实施建议

### 阶段 0：原型验证

使用 HTML 原型验证入口位置、侧栏宽度、回答结构、引用展示和用户理解度。

### 阶段 1：最小可用版本

选择 10–20 个高频 CRM 指标，完成知识治理；开发 Power BI 自定义视觉对象和企业问答 API；支持上下文、引用、反馈和审计。

### 阶段 2：扩展与治理

覆盖更多报表和指标，增加知识运营后台、冲突检测、版本提醒、质量评测和负责人闭环。

### 阶段 3：平台化

根据使用效果评估 Power BI Embedded 门户、跨报表会话、统一数据助手及其他 BI 场景。

## 17. 风险与待核准事项

以下事项不影响原型制作，但必须在生产开发前由相关负责人确认：

1. 最终选择 Power BI 自定义视觉对象还是 Power BI Embedded。
2. 自定义视觉对象是否允许访问外部企业 API，以及组织签名与发布流程。
3. 正式知识库产品、内容负责人和发布审核机制。
4. 复购等指标的真实业务规则及代码核验结果。
5. 企业大模型、向量检索、认证网关和日志平台选型。
6. 用户问题、答案及反馈的保存期限和合规要求。

## 18. 设计结论

首版体验采用“右下角 C Hive Lens 指标顾问 + 指标级快捷入口 + 右侧抽屉”。HTML 原型重点验证不离开报表的问答流程、筛选上下文实时联动、结构化口径回答、引用溯源、相关性分流与未回答问题闭环。生产实现优先评估 Power BI 自定义视觉对象；若必须实现跨画布的全局覆盖抽屉，则采用 Power BI Embedded 宿主方案。
